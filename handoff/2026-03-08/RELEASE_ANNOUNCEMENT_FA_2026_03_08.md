# اعلان انتشار - HESABPAK (2026-03-08)

نسخه جدید HESABPAK با هسته `ai_hp`، داشبورد فارسی `/developer` و مسیر کاملا بدون کدنویسی آماده بهره برداری است.
مدل پایه `qwen3.5:9b`، انتخاب مدل Local/Online، failover خودکار و Self-Completion فعال شده‌اند.
نمایش زنده وضعیت Core/Agentها و readiness اپراتور (با امتیاز رنگی) در UI در دسترس است.
کاربر نهایی فقط از `install.bat`، `start.bat` و `stop.bat` + داشبورد استفاده می‌کند.
شاخه انتشار آماده PR: `copilot/readiness-ui-hardening-publish`.
