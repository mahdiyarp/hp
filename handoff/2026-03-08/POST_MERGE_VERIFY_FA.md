# Post-Merge Verify - 2026-03-08

این چک‌لیست برای تایید نهایی بعد از Merge آماده شده است.

## 5 تست حیاتی

1. سرویس‌ها بالا هستند
- [ ] `start.bat` بدون خطای بحرانی اجرا شد.
- [ ] داشبورد در `http://localhost:8880/developer` باز می‌شود.

2. سلامت Backend
- [ ] Health/Status سبز است.
- [ ] Endpointهای core کلیدی پاسخ `200` می‌دهند.

3. readiness در UI
- [ ] کارت Smart Control Center مقدار readiness را نشان می‌دهد.
- [ ] score در بازه `0..100` نمایش داده می‌شود.
- [ ] رنگ score مطابق آستانه‌ها درست است.

4. Realtime Core/Frontend
- [ ] آپدیت زنده رویدادها در `/developer` دریافت می‌شود.
- [ ] بدون refresh دستی، وضعیت core به‌روز می‌شود.

5. مسیر کاربر بدون کدنویسی
- [ ] سناریوی اپراتور فقط با `install/start/stop` قابل اجرا است.
- [ ] نیازی به اجرای دستی تست/دستور فنی توسط کاربر نهایی نیست.

## نتیجه

- [ ] انتشار production-ready تایید شد.
