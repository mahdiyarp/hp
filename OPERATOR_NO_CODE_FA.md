# راهنمای اپراتور بدون کدنویسی (HESABPAK)

آخرین به‌روزرسانی: 2026-03-08

نسخه خیلی کوتاه (یک صفحه): `OPERATOR_ONE_PAGE_FA.md`
یادداشت انتشار اپراتور: `OPERATOR_RELEASE_NOTES_FA_2026_03_08.md`

## وضعیت فعلی سیستم (تایید شده)

- Core: آنلاین
- ماموریت: 6 از 6 کامل (`all_done=true`)
- مدل فعال هسته: `ai_hp / qwen3.5:9b`
- Ollama: متصل
- Smoke: همه چک‌های اصلی PASS

## فقط 3 فایل مورد نیاز کاربر

- `install.bat` : نصب/آماده‌سازی اولیه
- `start.bat` : اجرای کامل سیستم
- `stop.bat` : توقف امن سرویس‌ها

نکته: کاربر نهایی نیازی به ترمینال، ویرایش کد، یا اجرای دستور دستی ندارد.

## شروع سریع (بدون کدنویسی)

1. اگر بار اول است، روی `install.bat` دوبار کلیک کن.
2. روی `start.bat` دوبار کلیک کن.
3. مرورگر را باز کن و وارد این آدرس شو:
   - `http://localhost:8880`
4. برای کنترل پیشرفته، وارد داشبورد توسعه شو:
   - `http://localhost:8880/developer`

## کارهایی که از داخل UI انجام می‌شود

- بررسی وضعیت هسته (آنلاین/آفلاین، CPU/RAM، وضعیت Ollama)
- مشاهده Agentها و سلامت آن‌ها
- مشاهده Live Activity Feed
- اجرای چک سریع/عمیق سیستم
- اجرای Self-Completion
- Snapshot/Checkpoint
- سوییچ مدل‌های Local/Online (در صورت موجود بودن)
- ارسال فرمان به Core از پنل Manual Interaction

## مدل‌ها

- پیش‌فرض هسته: `ai_hp` با `qwen3.5:9b`
- مدل‌های Local (در صورت نصب):
  - `llama3`
  - `qwen2.5-coder`
  - `mistral`
  - `deepseek-coder`

## اتصال زنده Core و Frontend

- داشبورد به‌صورت Realtime وضعیت‌ها را به‌روزرسانی می‌کند.
- نیاز به Refresh دستی برای جریان اصلی وجود ندارد.

## میانبرهای جدید در کارت اپراتور

- دکمه `کپی آدرس داشبورد` برای کپی سریع مسیر `/developer` (بعد از موفقیت: `آدرس کپی شد`)
- دکمه `کپی نام فایل راهنما` برای کپی سریع نام راهنمای یک‌صفحه‌ای (بعد از موفقیت: `نام فایل کپی شد`)
- دکمه `کپی کل چک لیست` برای کپی سریع راهنمای کامل inline (بعد از موفقیت: `چک لیست کپی شد`)
- دکمه `کپی فقط مراحل بحران (4 مرحله)` برای کپی سریع سناریوی قطعی/قدیمی شدن وضعیت (بعد از موفقیت: `مراحل بحران کپی شد`)
- اگر پیام `مجوز Clipboard مرورگر را فعال کن` دیدی:
   - یک‌بار مجوز Clipboard مرورگر را فعال کن و دوباره دکمه کپی را بزن.

## اگر مشکلی دیدی (بدون کدنویسی)

1. روی `stop.bat` کلیک کن.
2. 5 ثانیه صبر کن.
3. دوباره `start.bat` را اجرا کن.
4. وارد `/developer` شو و دکمه "اجرای چک سریع سیستم" را بزن.

نکته مهم در داشبورد:

- اگر badge وضعیت Core زرد شد (`Core: آنلاین (قدیمی)`)، از همان کارت اپراتور دکمه `بروزرسانی وضعیت` را بزن.
- اگر بعد از Refresh همچنان زرد بود، یک بار `stop.bat` و سپس `start.bat` را اجرا کن.

## معنی badge وضعیت Core

- `Core: آنلاین` : وضعیت سالم و به‌روز است.
- `Core: آنلاین (قدیمی)` : داده دیر به‌روزرسانی شده؛ اول `بروزرسانی وضعیت` را بزن.
- `Core: آفلاین` : سرویس متوقف/قطع است؛ `stop.bat` و سپس `start.bat` را اجرا کن.

## مسیر بحران 4 مرحله ای

1. Core را در `/developer` چک کن.
2. اگر Core زرد بود: `بروزرسانی وضعیت` را یک بار بزن.
3. اگر هنوز زرد/آفلاین بود: `stop.bat` سپس `start.bat`.
4. دوباره `/developer` را باز کن و `Mission 6/6` را تایید کن.

## معیار سلامت قابل قبول

- صفحه اصلی باز می‌شود.
- مسیر `/developer` باز می‌شود.
- Core آنلاین است.
- Mission Audit برابر 6/6 است.
- چت دستیار پاسخ می‌دهد.

## معنی رنگ امتیاز آمادگی (در کارت مرکز کنترل هوشمند)

- سبز (`>= 80`) : وضعیت عملیاتی خوب است.
- زرد (`55 تا 79`) : نیاز به توجه دارد؛ چک سریع/عمیق اجرا شود.
- قرمز (`< 55`) : ریسک بالا؛ ابتدا چک عمیق، سپس پایدارسازی سریع.

## چک لیست 30 ثانیه ای روزانه

1. `http://localhost:8880` را باز کن.
2. وارد `/developer` شو.
3. وضعیت Core باید `آنلاین` باشد.
4. Mission Audit باید `6/6` باشد.
5. یک پیام کوتاه در چت بفرست و پاسخ را ببین.

## ممنوع برای کاربر نهایی

- ویرایش کد
- اجرای دستی اسکریپت‌های پیچیده
- تغییر مستقیم فایل‌های پیکربندی

این سند مخصوص اپراتور غیر فنی است.
