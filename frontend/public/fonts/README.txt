Drop Yekan webfont files here as webfonts (recommended: WOFF2). Name the primary file `Yekan.woff2`.

You can obtain Yekan from your licensed source. After adding `Yekan.woff2` the site will use it as the default font.
