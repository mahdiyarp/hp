Scaffold for both backend (FastAPI) and frontend (React + Vite).

Locations:
- `copilot/scaffold/backend` - minimal FastAPI app with pytest test
- `copilot/scaffold/frontend` - minimal React + Vite app

If you want CI workflows or branching, tell me and I will add them under `.github/workflows` and create a feature branch `copilot/scaffold`.
