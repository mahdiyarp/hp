# infra

Infrastructure-related files for the monorepo. The main `docker-compose.yml` lives at the repository root and will bring the services up.

Run from repository root:

```bash
docker compose up --build
```
