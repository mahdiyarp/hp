from __future__ import annotations

import argparse
import json
import re
import subprocess
from datetime import datetime, timezone
from pathlib import Path
from typing import Dict, List, Set

PROJECT_ROOT = Path(__file__).resolve().parents[2]
ROADMAP_DIR = PROJECT_ROOT / "roadmap"
GENERATED_PAGE_DIR = ROADMAP_DIR / "generated-page"
ROADMAP_JSON = ROADMAP_DIR / "roadmap.json"
STATUS_JSON = ROADMAP_DIR / "status.json"
ROADMAP_MD = ROADMAP_DIR / "roadmap.md"
LIVE_HTML = GENERATED_PAGE_DIR / "live.html"
PROGRESS_LOG = ROADMAP_DIR / "progress.log"
PHASE_DOCS_DIR = PROJECT_ROOT / "docs" / "phases"

# Default NeuroChainX roadmap phases (Fibonacci-weighted 10-stage plan).
DEFAULT_PHASES: List[Dict] = [
    {
        "id": 1,
        "code": "F01-ID",
        "name": "ظ‡ظˆغŒطھ ط¨ط°ط±",
        "status": "pending",
        "percent": 0,
        "fibonacci_weight": 1,
        "summary": "طھط¹ط±غŒظپ ظ‡ظˆغŒطھ ط§ظˆظ„غŒظ‡ NeuroChainXطŒ ط±ط§ظ‡ط¨ط±ط¯ DID ظˆ ظ…ط³غŒط±ظ‡ط§غŒ ط¨ط§ط²غŒط§ط¨غŒ ط§ظ…ظ†.",
        "dependencies": [],
        "outcomes": [
            "ظ…ط³طھظ†ط¯ ظ…ط´ط®طµط§طھ ظ‡ظˆغŒطھ (ع©ظ„غŒط¯طŒ ط´ظ†ط§ط³ظ‡طŒ ط§ط¯ط¹ط§ظ‡ط§)",
            "ط³غŒط§ط³طھ ط¨ط§ط²غŒط§ط¨غŒ ظˆ ظ†ع¯ظ‡ط¯ط§ط´طھ ع©ظ„غŒط¯ ط¨ط§ ظ…ط¯ظ„ طھظ‡ط¯غŒط¯",
            "ظ†ظ…ظˆظ†ظ‡ ط§ظˆظ„غŒظ‡ API طµط¯ظˆط± ظˆ ط§ط¹طھط¨ط§ط±ط³ظ†ط¬غŒ ط§ط¯ط¹ط§ظ‡ط§غŒ ظ‡ظˆغŒطھغŒ",
        ],
        "milestones": [
            {"id": "F1-M1", "task": "طھط¹ط±غŒظپ ظ¾ط§غŒظ‡â€Œظ‡ط§غŒ ظ‡ظˆغŒطھ NeuroChainX ظˆ ط§ظ…ط¶ط§غŒ ظ†ط§ظ…/ط¨ط±ظ†ط¯", "done": False},
            {"id": "F1-M2", "task": "ط·ط±ط§ط­غŒ ظ…ط¯غŒط±غŒطھ ع©ظ„غŒط¯ ع©غŒظپâ€Œظ¾ظˆظ„طŒ ط±ط¬غŒط³طھط±غŒ DID ظˆ ظ…ط³غŒط±ظ‡ط§غŒ ط¨ط§ط²غŒط§ط¨غŒ", "done": False},
            {"id": "F1-M3", "task": "ظ…ط¯ظ„ طھظ‡ط¯غŒط¯ ط¨ط±ط§غŒ ظ†ع¯ظ‡ط¯ط§ط´طھ ظ‡ظˆغŒطھ ظˆ ظ†ع¯ظ‡ط¨ط§ظ†ط§ظ† ط§ظˆظ„غŒظ‡", "done": False},
            {"id": "F1-M4", "task": "ظ†ظ…ظˆظ†ظ‡ ط§ظˆظ„غŒظ‡ API طµط¯ظˆط± ظˆ ط§ط¹طھط¨ط§ط±ط³ظ†ط¬غŒ ط§ط¯ط¹ط§غŒ ظ‡ظˆغŒطھغŒ", "done": False},
        ],
    },
    {
        "id": 2,
        "code": "F02-LEDGER",
        "name": "ط¯ظپطھط±ع©ظ„ ظ‡ط³طھظ‡",
        "status": "pending",
        "percent": 0,
        "fibonacci_weight": 1,
        "summary": "ط³ط§ط®طھ ظ…ط¯ظ„ ط¯ط§ط¯ظ‡ ط¯ظپطھط±ع©ظ„ ظˆ ط­ط§ظ„طھ ظ‚ط·ط¹غŒ ط³غŒط³طھظ….",
        "dependencies": ["F01-ID"],
        "outcomes": [
            "ط·ط±ط­ ط¯ظپطھط±ع©ظ„ ظˆ ظ¾ظˆط´ط´ طھط±ط§ع©ظ†ط´â€Œظ‡ط§ طھط¹ط±غŒظپ ط´ط¯",
            "ظ‚ظˆط§ظ†غŒظ† ط§ط¹طھط¨ط§ط±ط³ظ†ط¬غŒ ظˆ ظ‡ط´ ظ‚ط·ط¹غŒ ظ¾غŒط§ط¯ظ‡â€Œط³ط§ط²غŒ ط´ط¯",
            "ط«ط¨طھ ظˆظ‚ط§غŒط¹ ظ‚ط§ط¨ظ„ ظ…ظ…غŒط²غŒ ط¨ط±ط§غŒ ط±ط®ط¯ط§ط¯ظ‡ط§غŒ ط­غŒط§طھغŒ",
        ],
        "milestones": [
            {"id": "F2-M1", "task": "ط§ظ†طھط®ط§ط¨ ظ…ط¯ظ„ ط¯ط§ط¯ظ‡ ط¯ظپطھط±ع©ظ„ ظˆ ط´ظگظ…ط§غŒ ط¨ظ„ط§ع©/ظˆط±ظˆط¯غŒ", "done": False},
            {"id": "F2-M2", "task": "ظ¾غŒط§ط¯ظ‡â€Œط³ط§ط²غŒ ع†ط±ط®ظ‡ طھط±ط§ع©ظ†ط´ (ط§غŒط¬ط§ط¯/ط§ط¹طھط¨ط§ط±ط³ظ†ط¬غŒ/ط«ط¨طھ)", "done": False},
            {"id": "F2-M3", "task": "ظ‡ط´ ظ‚ط·ط¹غŒ ط­ط§ظ„طھ ظˆ ظ„ط§ع¯ ظ…ظ…غŒط²غŒ ظپظ‚ط·-ط§ظپط²ظˆط¯ظ†غŒ", "done": False},
            {"id": "F2-M4", "task": "طھط³طھ ظˆط§ط­ط¯/غŒع©ظ¾ط§ط±ع†ظ‡ ط¨ط±ط§غŒ ظ‡ط³طھظ‡ ط¯ظپطھط±ع©ظ„ ظˆ ط§ط¹طھط¨ط§ط±ط³ظ†ط¬غŒ", "done": False},
        ],
    },
    {
        "id": 3,
        "code": "F03-P2P",
        "name": "ظ‡ظ…ع¯ط§ظ…â€Œط³ط§ط²غŒ ظ‡ظ…طھط§â€Œط¨ظ‡â€Œظ‡ظ…طھط§",
        "status": "pending",
        "percent": 0,
        "fibonacci_weight": 2,
        "summary": "ط§غŒط¬ط§ط¯ ظ‡ظ…ع¯ط§ظ…â€Œط³ط§ط²غŒ ظ¾ط§غŒط¯ط§ط± ظ‡ظ…طھط§â€Œط¨ظ‡â€Œظ‡ظ…طھط§ ط¨ط§ ط­ظ„ طھط¹ط§ط±ط¶.",
        "dependencies": ["F02-LEDGER"],
        "outcomes": [
            "ط§ط³طھط±ط§طھعکغŒ ظ…ط´ P2P ط¨ط§ ع©ط´ظپ ظˆ ع¯ط§ط³ظ¾",
            "ط§ظ„ع¯ظˆغŒ ط­ظ„ طھط¹ط§ط±ط¶ (CRDT/ظ…ط±ع©ظ„) ظ…ط´ط®طµ",
            "ظ‡ظ…ع¯ط§ظ…â€Œط³ط§ط²غŒ ط¢ظپظ„ط§غŒظ†-ط§ظˆظ„ ظ‡ظ…ط±ط§ظ‡ ط¨ط§ ط³ظ†ط¬ظ‡â€Œظ‡ط§",
        ],
        "milestones": [
            {"id": "F3-M1", "task": "ط·ط±ط§ط­غŒ ع©ط´ظپ ظ‡ظ…طھط§ ظˆ ع¯ط§ط³ظ¾ ط¨ط±ط§غŒ LAN/WAN", "done": False},
            {"id": "F3-M2", "task": "ظ…ط³طھظ†ط¯ط³ط§ط²غŒ ط±ط§ظ‡ط¨ط±ط¯ ط­ظ„ طھط¹ط§ط±ط¶ (CRDT/ظ…ط±ع©ظ„ ط¯غŒظپ)", "done": False},
            {"id": "F3-M3", "task": "ظ‡ظ…ع¯ط§ظ…â€Œط³ط§ط²غŒ ط¢ظپظ„ط§غŒظ†-ط§ظˆظ„ ط¨ط§ ظ†ط´ط³طھâ€Œظ‡ط§غŒ ظ‚ط§ط¨ظ„ ط§ط²ط³ط±ع¯غŒط±غŒ", "done": False},
            {"id": "F3-M4", "task": "ط±ظ‡ع¯غŒط±غŒ observability ط¨ط±ط§غŒ ط¬ط±غŒط§ظ†â€Œظ‡ط§غŒ ظ‡ظ…ع¯ط§ظ…â€Œط³ط§ط²غŒ ط¯ط± ظ†ظˆط¯ ظ„ط¨ظ‡", "done": False},
        ],
    },
    {
        "id": 4,
        "code": "F04-CRM",
        "name": "ط¢ط؛ط§ط² CRM ظ‡ظˆط´ظ…ظ†ط¯",
        "status": "pending",
        "percent": 0,
        "fibonacci_weight": 3,
        "summary": "ط±ط§ظ‡â€Œط§ظ†ط¯ط§ط²غŒ ظ‡ط³طھظ‡ CRM ظ…ط±طھط¨ط· ط¨ط§ ظ‡ظˆغŒطھ ظˆ ط±ظˆغŒط¯ط§ط¯ظ‡ط§غŒ ط¯ظپطھط±ع©ظ„.",
        "dependencies": ["F01-ID", "F02-LEDGER", "F03-P2P"],
        "outcomes": [
            "ط´ظگظ…ط§غŒ CRM ظ…ط±طھط¨ط· ط¨ط§ ع¯ط±ط§ظپ ظ‡ظˆغŒطھ",
            "طھط§غŒظ…ظ„ط§غŒظ† ط±ظˆغŒط¯ط§ط¯ظ…ط­ظˆط± ظ…ط®ط§ط·ط¨ ط¨ط§ ط§ط¹ظ„ط§ظ†",
            "ظ¾ط§غŒظ„ظˆطھ UI ط§ظ¾ط±ط§طھظˆط± ط¨ط±ط§غŒ ط³ط±ظ†ط®/ظ…ط®ط§ط·ط¨",
        ],
        "milestones": [
            {"id": "F4-M1", "task": "طھط¹ط±غŒظپ ط´ظگظ…ط§غŒ ط¯ط§ط¯ظ‡ CRM ظ…طھطµظ„ ط¨ظ‡ ع¯ط±ط§ظپ ظ‡ظˆغŒطھ", "done": False},
            {"id": "F4-M2", "task": "طھط§غŒظ…ظ„ط§غŒظ† ط±ظˆغŒط¯ط§ط¯ظ…ط­ظˆط± ظ…ط®ط§ط·ط¨ ظˆ ط¯ط±غŒط§ظپطھ ط³غŒع¯ظ†ط§ظ„", "done": False},
            {"id": "F4-M3", "task": "ط³ط±ظˆغŒط³ ط±ط¶ط§غŒطھ ظˆ طھط±ط¬غŒط­ط§طھ ط§ط¹ظ„ط§ظ†", "done": False},
            {"id": "F4-M4", "task": "ط³ط·ط­ ط§ظˆظ„غŒظ‡ API CRM (ظ…ط®ط§ط·ط¨/ط³ط±ظ†ط®/طھط§غŒظ…ظ„ط§غŒظ†)", "done": False},
            {"id": "F4-M5", "task": "ظ¾ط§غŒظ„ظˆطھ UI ط§ظ¾ط±ط§طھظˆط± ط¨ط±ط§غŒ ظ†ظ…ط§ظ‡ط§غŒ ظ…ط®ط§ط·ط¨ ظˆ ط³ط±ظ†ط®", "done": False},
        ],
    },
    {
        "id": 5,
        "code": "F05-ECON",
        "name": "ظ„ط§غŒظ‡ ط§ظ‚طھطµط§ط¯غŒ",
        "status": "pending",
        "percent": 0,
        "fibonacci_weight": 5,
        "summary": "ظ…ط¹ط±ظپغŒ طھظˆع©ظ†ظˆظ…غŒع©ط³طŒ ظ‚غŒظ…طھâ€Œع¯ط°ط§ط±غŒ ظˆ ط±غŒظ„â€Œظ‡ط§غŒ طھط³ظˆغŒظ‡.",
        "dependencies": ["F02-LEDGER"],
        "outcomes": [
            "ظ…ط³طھظ†ط¯ طھظˆع©ظ†ظˆظ…غŒع©ط³ ظˆ ط¨ط±ظ†ط§ظ…ظ‡ ط§ظ†طھط´ط§ط±",
            "ظ…ظˆطھظˆط± ع©ط§ط±ظ…ط²ط¯ ظˆ ط³ظ†ط¬ط´ ظ…طھطµظ„ ط¨ظ‡ ط¯ظپطھط±ع©ظ„",
            "ط±طµط¯ ط®ط²ط§ظ†ظ‡ ظˆ ظ¾ط±ط¯ط§ط®طھâ€Œظ‡ط§",
        ],
        "milestones": [
            {"id": "F5-M1", "task": "ظ…ط³طھظ†ط¯ طھظˆع©ظ†ظˆظ…غŒع©ط³طŒ ط²ظ…ط§ظ†â€Œط¨ظ†ط¯غŒ ط§ظ†طھط´ط§ط± ظˆ ظ…ط¯ظ„ ظ…ط´ظˆظ‚", "done": False},
            {"id": "F5-M2", "task": "ط§ظˆط±ط§ع©ظ„ ظ‚غŒظ…طھ/FX ظˆ ظ…ط­ط¯ظˆط¯غŒطھ ط±غŒط³ع©", "done": False},
            {"id": "F5-M3", "task": "ظ…ظˆطھظˆط± ع©ط§ط±ظ…ط²ط¯ ط¯ظپطھط±ع©ظ„ ظˆ ظ‚ظ„ط§ط¨â€Œظ‡ط§غŒ ط³ظ†ط¬ط´", "done": False},
            {"id": "F5-M4", "task": "ط¬ط±غŒط§ظ†â€Œظ‡ط§غŒ طھط³ظˆغŒظ‡ ظ¾ط±ط¯ط§ط®طھ ط±ظˆغŒ ط±غŒظ„ ط¯ط§ط®ظ„غŒ/ط®ط§ط±ط¬غŒ", "done": False},
            {"id": "F5-M5", "task": "ط±طµط¯ ط®ط²ط§ظ†ظ‡ ظˆ ط¯ط§ط´ط¨ظˆط±ط¯ظ‡ط§غŒ ط­ط³ط§ط¨ط¯ط§ط±غŒ", "done": False},
        ],
    },
    {
        "id": 6,
        "code": "F06-AIOBS",
        "name": "ط±طµط¯ ظ‡ظˆط´ ظ…طµظ†ظˆط¹غŒ",
        "status": "pending",
        "percent": 0,
        "fibonacci_weight": 8,
        "summary": "ط¬ظ…ط¹â€Œط¢ظˆط±غŒ طھظ„ظ…طھط±غŒ ظˆ ط¢ظ…ط§ط¯ظ‡â€Œط³ط§ط²غŒ ط¯ط§ط¯ظ‡ ط¨ط±ط§غŒ ط¨غŒظ†ط´ ظˆ ط§غŒظ…ظ†غŒ AI.",
        "dependencies": ["F03-P2P", "F05-ECON"],
        "outcomes": [
            "ظ…ط´ط®طµط§طھ طھظ„ظ…طھط±غŒ ط¨ط§ ظ‚ظˆط§ط¹ط¯ ط­ط±غŒظ… ط®طµظˆطµغŒ ظˆ ط­ط°ظپ ط¯ط§ط¯ظ‡ ط­ط³ط§ط³",
            "ط®ط· ظ„ظˆظ„ظ‡ ع©ط´ظپ ظ†ط§ظ‡ظ†ط¬ط§ط±غŒ ظˆ ط³غŒع¯ظ†ط§ظ„",
            "ط­ظ„ظ‚ظ‡ ط¨ط§ط²ط®ظˆط±ط¯ ط§ظ†ط³ط§ظ†غŒ ط¯ط± ط­ط§ع©ظ…غŒطھ",
        ],
        "milestones": [
            {"id": "F6-M1", "task": "ظ…ط´ط®طµط§طھ طھظ„ظ…طھط±غŒ ط¨ط±ط§غŒ ظ†ط§ط¸ط±ط§ظ† AI ظˆ ظ‚ظˆط§ط¹ط¯ ط­ط°ظپ ط¯ط§ط¯ظ‡", "done": False},
            {"id": "F6-M2", "task": "ط¨ط±ع†ط³ط¨â€Œع¯ط°ط§ط±غŒ ظˆ ظ†ط§ط´ظ†ط§ط³â€Œط³ط§ط²غŒ ط¯ط§ط¯ظ‡ ط¨ط±ط§غŒ ط³غŒع¯ظ†ط§ظ„â€Œظ‡ط§غŒ ط¢ظ…ظˆط²ط´غŒ", "done": False},
            {"id": "F6-M3", "task": "ط§ظ†طھط®ط§ط¨ ظ…ط¯ظ„ ظ¾ط§غŒظ‡ ظˆ ظ‡ط§ط±ظ†ط³ ط§ط±ط²غŒط§ط¨غŒ", "done": False},
            {"id": "F6-M4", "task": "ط®ط· ظ„ظˆظ„ظ‡ ع©ط´ظپ ظ†ط§ظ‡ظ†ط¬ط§ط±غŒ (ظ†ظ…ظˆظ†ظ‡ ظ…ظپظ‡ظˆظ…غŒ)", "done": False},
            {"id": "F6-M5", "task": "ط§ط¨ط²ط§ط± ط­ظ„ظ‚ظ‡ ط¨ط§ط²ط®ظˆط±ط¯ ط¨ط§ ط§ظ†ط³ط§ظ† ط¯ط± ط­ظ„ظ‚ظ‡", "done": False},
        ],
    },
    {
        "id": 7,
        "code": "F07-BIZENG",
        "name": "ظ…ظˆطھظˆط± ع©ط³ط¨â€Œظˆع©ط§ط± غŒع©ظ¾ط§ط±ع†ظ‡",
        "status": "pending",
        "percent": 0,
        "fibonacci_weight": 13,
        "summary": "ط§غŒط¬ط§ط¯ ط§ط±ع©ط³طھط±ط§ط³غŒظˆظ†طŒ ظ‚ظˆط§ظ†غŒظ† ظˆ API ط¨ط±ط§غŒ ظ¾غŒظˆظ†ط¯ ظ‡ظˆغŒطھطŒ CRM ظˆ ط§ظ‚طھطµط§ط¯.",
        "dependencies": ["F04-CRM", "F05-ECON", "F06-AIOBS"],
        "outcomes": [
            "ظ…ظˆطھظˆط± ظ‚ظˆط§ظ†غŒظ† ط±ظˆغŒ ط±ظˆغŒط¯ط§ط¯ظ‡ط§غŒ ظ‡ظˆغŒطھ/CRM/ط§ظ‚طھطµط§ط¯غŒ",
            "ط§ط±ع©ط³طھط±غŒطھظˆط± ع¯ط±ط¯ط´â€Œع©ط§ط± ط¨ط§ ط²ظ…ط§ظ†â€Œط¨ظ†ط¯غŒ ظˆ طھع©ط±ط§ط±",
            "ط§ظ„ع¯ظˆظ‡ط§غŒ ط¯ط±ع¯ط§ظ‡ API ط¨ط±ط§غŒ ط´ط±ع©ط§",
        ],
        "milestones": [
            {"id": "F7-M1", "task": "ظ…ظˆطھظˆط± ظ‚ظˆط§ظ†غŒظ† ظ†ط³ط®ظ‡ غ± ط±ظˆغŒ ط±ظˆغŒط¯ط§ط¯ظ‡ط§غŒ ظ‡ظˆغŒطھ/CRM/ط§ظ‚طھطµط§ط¯غŒ", "done": False},
            {"id": "F7-M2", "task": "ط§ط±ع©ط³طھط±غŒطھظˆط± ع¯ط±ط¯ط´â€Œع©ط§ط± ط¨ط±ط§غŒ ط±ظˆغŒط¯ط§ط¯ظ‡ط§/ع©ط§ط±ظ‡ط§ ط¨ط§ طھع©ط±ط§ط±ظ¾ط°غŒط±غŒ", "done": False},
            {"id": "F7-M3", "task": "ط§ظ„ع¯ظˆظ‡ط§غŒ ط¯ط±ع¯ط§ظ‡ API طھط±ع©غŒط¨غŒ ط¨ط±ط§غŒ ط´ط±ع©ط§", "done": False},
            {"id": "F7-M4", "task": "ع©طھط§ط¨ط®ط§ظ†ظ‡ طھظ…ظ¾ظ„غŒطھ ط¨ط±ط§غŒ ع¯ط±ط¯ط´â€Œع©ط§ط±ظ‡ط§غŒ ط¹ظ…ظˆط¯غŒ", "done": False},
            {"id": "F7-M5", "task": "ط¨ظ†ع†ظ…ط§ط±ع© ع©ط§ط±ط§غŒغŒ ظˆ ظ…ظ‚غŒط§ط³â€Œظ¾ط°غŒط±غŒ ط¨ط§ SLI", "done": False},
        ],
    },
    {
        "id": 8,
        "code": "F08-MKT-INTEROP",
        "name": "ط¨ط§ط²ط§ط± ظˆ ط¨غŒظ†â€Œط¹ظ…ظ„غŒط§طھغŒ",
        "status": "pending",
        "percent": 0,
        "fibonacci_weight": 21,
        "summary": "ط¨ط§ط² ع©ط±ط¯ظ† ظ¾ظ„طھظپط±ظ… ط¨ط±ط§غŒ ط´ط±ع©ط§ ط¨ط§ ط§طھطµط§ظ„â€Œظ¾ط°غŒط±غŒ ظˆ ظ…ط¯ظ„ ط¯ط±ط¢ظ…ط¯غŒ.",
        "dependencies": ["F07-BIZENG"],
        "outcomes": [
            "SDK ط§طھطµط§ظ„ ظˆ ظ…ط³غŒط± ع¯ظˆط§ظ‡غŒ",
            "ع†ط±ط®ظ‡ ط¹ظ…ط± ظ„غŒط³طھغŒظ†ع¯ ظ…ط§ط±ع©طھâ€Œظ¾ظ„غŒط³ ظˆ ظ…ط¯ظ„ طھظ‚ط³غŒظ… ط¯ط±ط¢ظ…ط¯",
            "ط¨ط§ط²ط¨غŒظ†غŒ ط§ظ…ظ†غŒطھغŒ ط¨ط±ط§غŒ غŒع©ظ¾ط§ط±ع†ظ‡â€Œط³ط§ط²غŒâ€Œظ‡ط§",
        ],
        "milestones": [
            {"id": "F8-M1", "task": "ع†ع©â€Œظ„غŒط³طھ ط¬ط°ط¨ ط´ط±غŒع© ظˆ ط±ط§ظ‡ظ†ظ…ط§غŒ ط¯ظ‚طھ ظ„ط§ط²ظ…", "done": False},
            {"id": "F8-M2", "task": "SDK ط§طھطµط§ظ„ ط¨ط§ ظ†ظ…ظˆظ†ظ‡ ع©ط§ظ†ع©طھظˆط± (ERP/CRM/ظ¾ط±ط¯ط§ط®طھ)", "done": False},
            {"id": "F8-M3", "task": "ط´ظگظ…ط§ ظˆ ع†ط±ط®ظ‡ ط¹ظ…ط± ظ„غŒط³طھغŒظ†ع¯ ظ…ط§ط±ع©طھâ€Œظ¾ظ„غŒط³", "done": False},
            {"id": "F8-M4", "task": "ظ…ط³طھظ†ط¯ط³ط§ط²غŒ ظ…ط¯ظ„ طھظ‚ط³غŒظ… ط¯ط±ط¢ظ…ط¯ ظˆ طھط³ظˆغŒظ‡", "done": False},
            {"id": "F8-M5", "task": "طھط³طھ ط§ظ†ط·ط¨ط§ظ‚ ط¨غŒظ†â€Œط¹ظ…ظ„غŒط§طھغŒ ظˆ ظ…ط³غŒط± ع¯ظˆط§ظ‡غŒ", "done": False},
            {"id": "F8-M6", "task": "ط¨ط§ط²ط¨غŒظ†غŒ ط§ظ…ظ†غŒطھغŒ ط¨ط±ط§غŒ ع©ط§ظ†ع©طھظˆط±ظ‡ط§ ظˆ ط³ظ†ط§ط±غŒظˆظ‡ط§غŒ طھظ‡ط¯غŒط¯", "done": False},
        ],
    },
    {
        "id": 9,
        "code": "F09-GOV-DAO",
        "name": "ط­ط§ع©ظ…غŒطھ DAO",
        "status": "pending",
        "percent": 0,
        "fibonacci_weight": 34,
        "summary": "طھط¹ط±غŒظپ ط³ط§ط²ظˆع©ط§ط± طھطµظ…غŒظ…â€Œع¯غŒط±غŒطŒ ع©ظ†طھط±ظ„ ط®ط²ط§ظ†ظ‡ ظˆ ظ…ط´ط§ط±ع©طھ.",
        "dependencies": ["F08-MKT-INTEROP", "F05-ECON"],
        "outcomes": [
            "ظ…ظ†ط´ظˆط± ط­ط§ع©ظ…غŒطھ ظˆ ط¯ط§ظ…ظ†ظ‡â€Œظ‡ط§غŒ طھطµظ…غŒظ…â€Œع¯غŒط±غŒ",
            "ط±ط£غŒâ€Œط¯ظ‡غŒطŒ طھظپظˆغŒط¶ ظˆ ط³ط§ط²ظˆع©ط§ط± ط§ط¹طھط¨ط§ط±",
            "ع©ظ†طھط±ظ„ ط®ط²ط§ظ†ظ‡ ط¨ط§ ط³غŒط§ط³طھâ€Œظ‡ط§غŒ ظ‚ط§ط¨ظ„ ظ…ظ…غŒط²غŒ",
        ],
        "milestones": [
            {"id": "F9-M1", "task": "طھط¹ط±غŒظپ ظ…ظ†ط´ظˆط± ط­ط§ع©ظ…غŒطھ ظˆ ظپط¶ط§ظ‡ط§غŒ طھطµظ…غŒظ…", "done": False},
            {"id": "F9-M2", "task": "ظ‚ظˆط§ظ†غŒظ† ط±ط£غŒâ€Œط¯ظ‡غŒطŒ ظ†طµط§ط¨ ظˆ طھظپظˆغŒط¶", "done": False},
            {"id": "F9-M3", "task": "ط³غŒط³طھظ… ظ†ظ…ط§غŒظ†ط¯ظ‡ ظˆ ط³غŒع¯ظ†ط§ظ„â€Œظ‡ط§غŒ ط§ط¹طھط¨ط§ط±", "done": False},
            {"id": "F9-M4", "task": "ع©ظ†طھط±ظ„ ط®ط²ط§ظ†ظ‡طŒ ط³غŒط§ط³طھ ظ‡ط²غŒظ†ظ‡ ظˆ ظ…ظ…غŒط²غŒ", "done": False},
            {"id": "F9-M5", "task": "ط·ط±ط­ ظ¾ظ„ ط±ظˆغŒâ€Œط²ظ†ط¬غŒط±ظ‡/ط®ط§ط±ط¬â€Œط²ظ†ط¬غŒط±ظ‡ ط¨ط±ط§غŒ ط¯ط§ط¯ظ‡ ط­ط§ع©ظ…غŒطھ", "done": False},
        ],
    },
    {
        "id": 10,
        "code": "F10-AUTO-ECO",
        "name": "ط§ع©ظˆط³غŒط³طھظ… ط®ظˆط¯طھع©ط§ظ…ظ„غŒ",
        "status": "pending",
        "percent": 0,
        "fibonacci_weight": 55,
        "summary": "ط³ط§ط®طھ ط­ظ„ظ‚ظ‡â€Œظ‡ط§غŒ ط®ظˆط¯ع©ط§ط±طŒ ظ†ظˆط³ط§ط²غŒ ظ…ط³طھظ†ط¯ط§طھ ظˆ ط±غŒظ„â€Œظ‡ط§غŒ ط§غŒظ…ظ†غŒ ط¨ط±ط§غŒ طھع©ط§ظ…ظ„ ظ…ط¯ط§ظˆظ….",
        "dependencies": ["F09-GOV-DAO", "F07-BIZENG"],
        "outcomes": [
            "ط¹ط§ظ…ظ„ ظ†ظ‚ط´ظ‡â€Œط±ط§ظ‡ ط¨ط§ ع¯ط§ط±ط¯ط±غŒظ„ ظˆ observability",
            "ط®ط· ظ†ظˆط³ط§ط²غŒ ط®ظˆط¯ع©ط§ط± ظ…ط³طھظ†ط¯ط§طھ ظˆ ط§ظ†طھط´ط§ط±",
            "ط³ظ†ط¬ظ‡â€Œظ‡ط§غŒ ط³ظ„ط§ظ…طھ ط§ع©ظˆط³غŒط³طھظ… ظˆ ط¢ط²ظ…ط§غŒط´â€Œظ¾ط°غŒط±غŒ",
        ],
        "milestones": [
            {"id": "F10-M1", "task": "ط¹ط§ظ…ظ„ ظ†ظ‚ط´ظ‡â€Œط±ط§ظ‡ ظ†ط³ط®ظ‡ غ± ط¨ط§ ع¯ط§ط±ط¯ط±غŒظ„ ط¨ط±ط§غŒ ط®ظˆط¯ع©ط§ط±ط³ط§ط²غŒ", "done": False},
            {"id": "F10-M2", "task": "ط®ط· ط®ظˆط¯ط¨ظ‡â€Œط±ظˆط²ط±ط³ط§ظ†غŒ ظ…ط³طھظ†ط¯ط§طھ ظˆ ط§ظ†طھط´ط§ط±", "done": False},
            {"id": "F10-M3", "task": "ط³ظ†ط¯ط¨ط§ع©ط³ ط¢ط²ظ…ط§غŒط´ ط¨ط§ ط¯ط±ظˆط§ط²ظ‡ ط§ظ†طھط´ط§ط± طھط¯ط±غŒط¬غŒ", "done": False},
            {"id": "F10-M4", "task": "ط³ظ†ط¬ظ‡â€Œظ‡ط§غŒ طھع©ط§ظ…ظ„ (ظپط¹ط§ظ„â€Œط³ط§ط²غŒ/ظ†ع¯ظ‡ط¯ط§ط´طھ/ط³ظ„ط§ظ…طھ ط´ط±غŒع©)", "done": False},
            {"id": "F10-M5", "task": "ظ‡ظ…ع©ط§ط± AI ط¨ط±ط§غŒ ط³ط§ط²ظ†ط¯ع¯ط§ظ† ظˆ غŒع©ظ¾ط§ط±ع†ظ‡â€Œط³ط§ط²غŒ ط´ط±ع©ط§", "done": False},
        ],
    },
]


def clone_default_phases() -> List[Dict]:
    return json.loads(json.dumps(DEFAULT_PHASES))


def ensure_dirs() -> None:
    ROADMAP_DIR.mkdir(parents=True, exist_ok=True)
    GENERATED_PAGE_DIR.mkdir(parents=True, exist_ok=True)
    PHASE_DOCS_DIR.mkdir(parents=True, exist_ok=True)


def slugify(value: str) -> str:
    cleaned = re.sub(r"[^\w-]+", "-", value, flags=re.UNICODE)
    cleaned = re.sub(r"-+", "-", cleaned).strip("-")
    return cleaned or "phase"


def parse_markers_from_text(text: str) -> Set[str]:
    pattern = re.compile(r"DONE:([A-Za-z0-9._-]+)", re.IGNORECASE)
    return {m.group(1).upper() for m in pattern.finditer(text or "")}


def extract_done_markers_from_commits(limit: int = 50) -> Set[str]:
    try:
        proc = subprocess.run(
            ["git", "log", f"-{limit}", "--pretty=%B"],
            cwd=PROJECT_ROOT,
            capture_output=True,
            text=True,
            check=True,
        )
    except Exception:
        return set()
    return parse_markers_from_text(proc.stdout)


def load_roadmap() -> Dict:
    if ROADMAP_JSON.exists():
        try:
            data = json.loads(ROADMAP_JSON.read_text(encoding="utf-8"))
        except Exception:
            data = {}
    else:
        data = {}
    if "phases" not in data:
        data["phases"] = clone_default_phases()
    data.setdefault("project", "NeuroChainX")
    data.setdefault("version", "v1")
    data.setdefault("notes", "Auto-generated by roadmap_sync")
    return data


def normalize_roadmap(roadmap: Dict) -> None:
    for phase in roadmap.get("phases", []):
        phase.setdefault("status", "pending")
        phase.setdefault("percent", 0)
        phase.setdefault("fibonacci_weight", 1)
        phase.setdefault("dependencies", [])
        phase.setdefault("milestones", [])
        for m in phase["milestones"]:
            m["done"] = bool(m.get("done", False))
            m["task"] = m.get("task", "").strip()
    roadmap["phases"] = sorted(roadmap.get("phases", []), key=lambda p: p.get("id", 0))


def apply_done_markers(roadmap: Dict, markers: Set[str]) -> List[str]:
    applied: List[str] = []
    normalized = {m.upper().strip() for m in markers if m}
    if not normalized:
        return applied
    for phase in roadmap.get("phases", []):
        for m in phase.get("milestones", []):
            mid = str(m.get("id") or "").upper()
            if mid in normalized and not m.get("done"):
                m["done"] = True
                applied.append(mid)
    return applied


def recalc_progress(roadmap: Dict) -> Dict:
    phases = roadmap.get("phases", [])
    total_weights = sum(p.get("fibonacci_weight", 1) for p in phases) or 1
    total_milestones = 0
    completed_milestones = 0
    for phase in phases:
        milestones = phase.get("milestones", [])
        total_milestones += len(milestones)
        done_count = sum(1 for m in milestones if m.get("done"))
        completed_milestones += done_count
        percent = round((done_count / len(milestones)) * 100, 2) if milestones else 0
        phase["percent"] = percent
        phase["milestones_total"] = len(milestones)
        phase["milestones_done"] = done_count
        if done_count == 0:
            phase["status"] = "pending"
        elif done_count == len(milestones):
            phase["status"] = "completed"
        else:
            phase["status"] = "in_progress"
    weighted = (
        sum((p.get("percent", 0) * p.get("fibonacci_weight", 1)) for p in phases) / total_weights
        if phases
        else 0
    )
    milestone_progress = (completed_milestones / total_milestones * 100) if total_milestones else 0
    now_iso = datetime.now(timezone.utc).isoformat()
    roadmap["overall_progress"] = round(weighted, 2)
    roadmap["overall_weighted_progress"] = roadmap["overall_progress"]
    roadmap["overall_milestone_progress"] = round(milestone_progress, 2)
    roadmap["phases_completed"] = sum(1 for p in phases if p.get("status") == "completed")
    roadmap["phases_in_progress"] = sum(1 for p in phases if p.get("status") == "in_progress")
    roadmap["phases_pending"] = sum(1 for p in phases if p.get("status") == "pending")
    roadmap["total_milestones"] = total_milestones
    roadmap["completed_milestones"] = completed_milestones
    roadmap["updated_at"] = now_iso
    return roadmap


def build_status_payload(roadmap: Dict, applied_markers: Set[str], sources: List[str]) -> Dict:
    source_list = sources or ["auto"]
    return {
        "project": roadmap.get("project", "NeuroChainX"),
        "overall_progress": roadmap.get("overall_progress", 0),
        "overall_weighted_progress": roadmap.get("overall_weighted_progress", roadmap.get("overall_progress", 0)),
        "overall_milestone_progress": roadmap.get("overall_milestone_progress", 0),
        "phases_completed": roadmap.get("phases_completed", 0),
        "phases_in_progress": roadmap.get("phases_in_progress", 0),
        "phases_pending": roadmap.get("phases_pending", 0),
        "total_milestones": roadmap.get("total_milestones", 0),
        "completed_milestones": roadmap.get("completed_milestones", 0),
        "updated_at": roadmap.get("updated_at"),
        "applied_markers": sorted(applied_markers),
        "sources": source_list,
        "phases": [
            {
                "id": p.get("id"),
                "code": p.get("code"),
                "name": p.get("name"),
                "status": p.get("status"),
                "percent": p.get("percent"),
                "fibonacci_weight": p.get("fibonacci_weight"),
                "milestones_total": p.get("milestones_total", len(p.get("milestones", []))),
                "milestones_done": p.get("milestones_done", 0),
            }
            for p in roadmap.get("phases", [])
        ],
    }


def write_json(path: Path, payload: Dict) -> None:
    path.write_text(json.dumps(payload, indent=2, ensure_ascii=False), encoding="utf-8")


def write_roadmap_md(roadmap: Dict, status_payload: Dict) -> None:
    status_map = {"pending": "ط¯ط± ط§ظ†طھط¸ط§ط±", "in_progress": "ط¯ط± ط­ط§ظ„ ط§ظ†ط¬ط§ظ…", "completed": "طھع©ظ…غŒظ„\u200cط´ط¯ظ‡"}
    lines: List[str] = []
    lines.append("# ظ†ظ‚ط´ظ‡ ط±ط§ظ‡ NeuroChainX")
    lines.append("")
    lines.append(f"ط¢ط®ط±غŒظ† ط¨ظ‡\u200cط±ظˆط²ط±ط³ط§ظ†غŒ: {status_payload.get('updated_at')}")
    lines.append(
        f"ظ¾غŒط´ط±ظپطھ ظˆط²ظ†غŒ ع©ظ„غŒ: {status_payload.get('overall_progress', 0)}%"
    )
    lines.append(
        f"طھع©ظ…غŒظ„ ظ…ط§غŒظ„ط³طھظˆظ†: {status_payload.get('completed_milestones', 0)}/{status_payload.get('total_milestones', 0)} "
        f"({status_payload.get('overall_milestone_progress', 0)}%)"
    )
    lines.append("")
    lines.append("| ظپط§ط² | ظˆط¶ط¹غŒطھ | ظھ | ط§ظ†ط¬ط§ظ…/ع©ظ„ | ظˆط²ظ† |")
    lines.append("| --- | --- | --- | --- | --- |")
    for p in roadmap.get("phases", []):
        st = status_map.get(str(p.get("status") or "").lower(), p.get("status"))
        lines.append(
            f"| {p.get('id')} - {p.get('name')} | {st} | {p.get('percent')}% | "
            f"{p.get('milestones_done')}/{p.get('milestones_total')} | {p.get('fibonacci_weight')} |"
        )
    lines.append("")
    lines.append("## ط¬ط²ط¦غŒط§طھ ظپط§ط²ظ‡ط§")
    for p in roadmap.get("phases", []):
        lines.append(f"### {p.get('id'):02d} - {p.get('name')}")
        lines.append(
            f"ظˆط¶ط¹غŒطھ: {status_map.get(str(p.get('status') or '').lower(), p.get('status'))} ({p.get('percent')}%)طŒ ظˆط²ظ†: {p.get('fibonacci_weight')}طŒ "
            f"ظˆط§ط¨ط³طھع¯غŒ\u200cظ‡ط§: {', '.join(p.get('dependencies', []) or ['ط¨ط¯ظˆظ† ظˆط§ط¨ط³طھع¯غŒ'])}"
        )
        if p.get("summary"):
            lines.append("")
            lines.append(p.get("summary"))
        lines.append("")
        lines.append("ظ…ط§غŒظ„ط³طھظˆظ†\u200cظ‡ط§:")
        for m in p.get("milestones", []):
            prefix = "[x]" if m.get("done") else "[ ]"
            lines.append(f"- {prefix} {m.get('id')}: {m.get('task')}")
        if p.get("outcomes"):
            lines.append("")
            lines.append("ط®ط±ظˆط¬غŒ\u200cظ‡ط§غŒ ظ…ظˆط±ط¯ ط§ظ†طھط¸ط§ط±:")
            for o in p.get("outcomes", []):
                lines.append(f"- {o}")
        lines.append("")
    ROADMAP_MD.write_text("\n".join(lines), encoding="utf-8")


def write_phase_docs(roadmap: Dict) -> None:
    status_map = {"pending": "ط¯ط± ط§ظ†طھط¸ط§ط±", "in_progress": "ط¯ط± ط­ط§ظ„ ط§ظ†ط¬ط§ظ…", "completed": "طھع©ظ…غŒظ„\u200cط´ط¯ظ‡"}
    for phase in roadmap.get("phases", []):
        slug = slugify(phase.get("name", f"phase-{phase.get('id', '')}"))
        path = PHASE_DOCS_DIR / f"{phase.get('id'):02d}-{slug}.md"
        lines = [
            f"# ظپط§ط² {phase.get('id'):02d} - {phase.get('name')}",
            "",
            f"ظˆط¶ط¹غŒطھ: {status_map.get(str(phase.get('status') or '').lower(), phase.get('status'))} ({phase.get('percent')}%)طŒ ظˆط²ظ†: {phase.get('fibonacci_weight')}",
            f"ظˆط§ط¨ط³طھع¯غŒ\u200cظ‡ط§: {', '.join(phase.get('dependencies', []) or ['ط¨ط¯ظˆظ† ظˆط§ط¨ط³طھع¯غŒ'])}",
            "",
            "## ط®ظ„ط§طµظ‡",
            phase.get("summary", "â€”"),
            "",
            "## ظ…ط§غŒظ„ط³طھظˆظ†\u200cظ‡ط§",
        ]
        for m in phase.get("milestones", []):
            prefix = "[x]" if m.get("done") else "[ ]"
            lines.append(f"- {prefix} {m.get('id')}: {m.get('task')}")
        lines.append("")
        lines.append("## ط®ط±ظˆط¬غŒ\u200cظ‡ط§غŒ ظ…ظˆط±ط¯ ط§ظ†طھط¸ط§ط±")
        for o in phase.get("outcomes", []) or ["(ظ¾ط³ ط§ط² ظ…ط´ط®طµ ط´ط¯ظ† ط®ط±ظˆط¬غŒ\u200cظ‡ط§ طھع©ظ…غŒظ„ ط´ظˆط¯)"]:
            lines.append(f"- {o}")
        path.write_text("\n".join(lines) + "\n", encoding="utf-8")


def generate_live_html(roadmap: Dict, status_payload: Dict) -> None:
    payload = {"roadmap": roadmap, "status": status_payload}
    payload_json = json.dumps(payload, ensure_ascii=False).replace("</", "<\\/")
    html_template = """<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ظ†ظ‚ط´ظ‡ ط±ط§ظ‡ NeuroChainX - ط²ظ†ط¯ظ‡</title>
  <style>
    :root {
      --bg: #0b111a;
      --panel: #0f1724;
      --panel-2: #0b1220;
      --accent: #7c3aed;
      --accent-2: #22d3ee;
      --text: #e5e7eb;
      --muted: #94a3b8;
      --border: #1f2937;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: 'Vazirmatn', 'IRANSans', 'Space Grotesk', 'Inter', system-ui, -apple-system, sans-serif;
      background: radial-gradient(circle at 20% 20%, rgba(124,58,237,0.12), transparent 30%), radial-gradient(circle at 80% 0%, rgba(34,211,238,0.1), transparent 25%), var(--bg);
      color: var(--text);
      min-height: 100vh;
      padding: 24px;
      direction: rtl;
      text-align: right;
    }
    .shell {
      max-width: 1200px;
      margin: 0 auto;
    }
    .hero {
      background: linear-gradient(135deg, rgba(124,58,237,0.25), rgba(34,211,238,0.2));
      border: 1px solid var(--border);
      border-radius: 18px;
      padding: 20px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.35), 0 0 30px rgba(124,58,237,0.2);
    }
    .hero h1 { margin: 0 0 8px; letter-spacing: 0.5px; }
    .meta { display: flex; gap: 12px; flex-wrap: wrap; color: var(--muted); font-size: 14px; }
    .progress {
      position: relative;
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: 12px;
      margin: 20px 0 8px;
      height: 14px;
      overflow: hidden;
    }
    .progress span {
      position: absolute;
      inset: 0;
      background: linear-gradient(90deg, var(--accent), var(--accent-2));
      box-shadow: 0 0 24px rgba(124,58,237,0.3);
      transition: width 0.6s ease;
    }
    .cards { margin-top: 18px; display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 14px; }
    .card {
      background: var(--panel-2);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 14px;
      box-shadow: 0 14px 40px rgba(0,0,0,0.25);
    }
    .card header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 8px;
    }
    .pill {
      padding: 2px 10px;
      border-radius: 999px;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      border: 1px solid var(--border);
      background: rgba(255,255,255,0.05);
    }
    .pill.pending { color: #fbbf24; border-color: #f59e0b33; }
    .pill.in_progress { color: #60a5fa; border-color: #3b82f633; }
    .pill.completed { color: #34d399; border-color: #10b98133; }
    .milestones { margin-top: 8px; padding-left: 0; list-style: none; display: grid; gap: 6px; }
    .milestones li { display: flex; gap: 8px; align-items: flex-start; color: var(--muted); }
    .tick {
      width: 18px; height: 18px; border-radius: 4px; border: 1px solid var(--border); display: grid; place-items: center; font-size: 11px;
      background: rgba(124,58,237,0.12);
    }
    .tick.done { background: linear-gradient(135deg, #34d399, #22c55e); color: #0b111a; border-color: #34d399; }
    .footer { margin-top: 6px; color: var(--muted); font-size: 12px; }
    .last-updated { color: var(--muted); font-size: 13px; }
    @media (max-width: 640px) {
      body { padding: 14px; }
      .cards { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  <div class="shell">
    <div class="hero">
      <h1>ظ†ظ‚ط´ظ‡ ط±ط§ظ‡ NeuroChainX</h1>
      <div class="meta">
        <div id="overall-percent">--%</div>
        <div id="milestone-progress">--</div>
        <div id="phase-counts">--</div>
      </div>
      <div class="progress"><span id="overall-bar" style="width: 0%"></span></div>
      <div class="last-updated" id="last-updated">Last sync: --</div>
    </div>
    <div class="cards" id="phase-list"></div>
  </div>

<script>
const initialPayload = __PAYLOAD__;

function statusLabel(status) {
  const map = {
    pending: 'ط¯ط± ط§ظ†طھط¸ط§ط±',
    in_progress: 'ط¯ط± ط­ط§ظ„ ط§ظ†ط¬ط§ظ…',
    completed: 'طھع©ظ…غŒظ„\u200cط´ط¯ظ‡'
  };
  const key = (status || '').toLowerCase();
  return map[key] || status || 'ظ†ط§ظ…ط´ط®طµ';
}

function percent(val) {
  const n = Number(val || 0);
  return Number.isFinite(n) ? Math.max(0, Math.min(100, n)) : 0;
}

function render(roadmap, status) {
  const phases = (roadmap && roadmap.phases) || [];
  const list = document.getElementById('phase-list');
  list.innerHTML = '';
  phases.forEach(p => {
    const card = document.createElement('div');
    card.className = 'card';
    const done = p.milestones_done || 0;
    const total = p.milestones_total || (p.milestones ? p.milestones.length : 0) || 0;
    const pct = percent(p.percent);
    card.innerHTML = `
      <header>
        <div>
          <div style="font-weight: 600">${String(p.id).padStart(2, '0')} - ${p.name || ''}</div>
          <div style="color: var(--muted); font-size: 12px;">${p.code || ''} - ظˆط²ظ† ${p.fibonacci_weight || 0} - ${done}/${total} ظ…ط§غŒظ„ط³طھظˆظ†</div>
        </div>
        <span class="pill ${p.status || 'pending'}">${statusLabel(p.status)}</span>
      </header>
      <div class="progress" style="margin: 10px 0 8px; height: 8px;"><span style="width:${pct}%"></span></div>
      <ul class="milestones">
        ${((p.milestones || []).map(m => {
          const flag = m.done ? 'done' : '';
          const tick = m.done ? '&#10003;' : '';
          return `<li><span class="tick ${flag}">${tick}</span><div><strong>${m.id}</strong> - ${m.task}</div></li>`;
        }).join(''))}
      </ul>
      <div class="footer">${p.summary || ''}</div>
    `;
    list.appendChild(card);
  });

  const overall = percent(status && status.overall_progress);
  document.getElementById('overall-bar').style.width = overall + '%';
  document.getElementById('overall-percent').textContent = `ظ¾غŒط´ط±ظپطھ ظˆط²ظ†غŒ: ${overall.toFixed(1)}%`;
  document.getElementById('milestone-progress').textContent = `ظ…ط§غŒظ„ط³طھظˆظ†: ${(status && status.completed_milestones) || 0}/${(status && status.total_milestones) || 0}`;
  document.getElementById('phase-counts').textContent = `ظپط§ط²ظ‡ط§ ط¯ط± ط§ظ†طھط¸ط§ط±/ط¯ط± ط­ط§ظ„ ط§ظ†ط¬ط§ظ…/طھع©ظ…غŒظ„: ${(status && status.phases_pending) || 0} / ${(status && status.phases_in_progress) || 0} / ${(status && status.phases_completed) || 0}`;
  document.getElementById('last-updated').textContent = 'ط¢ط®ط±غŒظ† ظ‡ظ…ع¯ط§ظ…\u200cط³ط§ط²غŒ: ' + (status && status.updated_at ? status.updated_at : 'ظ†ط§ظ…ط´ط®طµ');
}

async function refresh() {
  try {
    const [roadmapRes, statusRes] = await Promise.all([
      fetch('/roadmap/json'),
      fetch('/roadmap/status')
    ]);
    if (!roadmapRes.ok || !statusRes.ok) return;
    const roadmap = await roadmapRes.json();
    const status = await statusRes.json();
    render(roadmap, status);
  } catch (err) {
    console.warn('Roadmap live refresh failed', err);
  }
}

render(initialPayload.roadmap || {}, initialPayload.status || {});
refresh();
setInterval(refresh, 15000);
</script>
</body>
</html>
"""
    LIVE_HTML.write_text(html_template.replace("__PAYLOAD__", payload_json), encoding="utf-8")


def append_progress_log(status_payload: Dict, applied_markers: Set[str], sources: List[str]) -> None:
    ts = status_payload.get("updated_at") or datetime.now(timezone.utc).isoformat()
    markers = ", ".join(sorted(applied_markers)) if applied_markers else "none"
    src = ";".join(sources or ["auto"])
    line = (
        f"{ts}\toverall={status_payload.get('overall_progress', 0)}%"
        f"\tmilestones={status_payload.get('completed_milestones', 0)}/{status_payload.get('total_milestones', 0)}"
        f"\tsources={src}\tapplied={markers}\n"
    )
    with PROGRESS_LOG.open("a", encoding="utf-8") as fh:
        fh.write(line)


def run_sync(args: argparse.Namespace) -> Dict:
    ensure_dirs()
    roadmap = load_roadmap()
    if args.reset_defaults or not roadmap.get("phases"):
        roadmap["phases"] = clone_default_phases()
    normalize_roadmap(roadmap)
    markers: Set[str] = set()
    sources: List[str] = []

    if not args.no_commit_scan:
        commit_markers = extract_done_markers_from_commits(args.commit_limit)
        if commit_markers:
            markers.update(commit_markers)
            sources.append(f"git:{args.commit_limit}")

    if args.text:
        text_markers = parse_markers_from_text(args.text)
        if text_markers:
            markers.update(text_markers)
            sources.append("text")

    if args.markers:
        manual = set()
        for m in args.markers:
            m = m.strip()
            if not m:
                continue
            if m.upper().startswith("DONE:"):
                manual.add(m.split("DONE:", 1)[1].strip().upper())
            else:
                manual.add(m.upper())
        if manual:
            markers.update(manual)
            sources.append("manual")

    applied = apply_done_markers(roadmap, markers)
    recalc_progress(roadmap)
    status_payload = build_status_payload(roadmap, markers, sources)

    if args.dry_run:
        return {"roadmap": roadmap, "status": status_payload, "applied": applied}

    write_json(ROADMAP_JSON, roadmap)
    write_json(STATUS_JSON, status_payload)
    write_roadmap_md(roadmap, status_payload)
    write_phase_docs(roadmap)
    generate_live_html(roadmap, status_payload)
    append_progress_log(status_payload, markers, sources)

    return {"roadmap": roadmap, "status": status_payload, "applied": applied}


def main(argv: List[str] | None = None) -> None:
    parser = argparse.ArgumentParser(
        description="Sync NeuroChainX roadmap artifacts from milestone markers."
    )
    parser.add_argument(
        "--markers", nargs="*", default=[], help="Explicit DONE:<milestone_id> markers."
    )
    parser.add_argument(
        "--text", default="", help="Parse DONE markers from a text blob or commit message."
    )
    parser.add_argument(
        "--commit-limit",
        type=int,
        default=50,
        help="Number of commits to scan for DONE markers.",
    )
    parser.add_argument("--no-commit-scan", action="store_true", help="Skip git commit scanning.")
    parser.add_argument("--dry-run", action="store_true", help="Compute without writing files.")
    parser.add_argument(
        "--reset-defaults",
        action="store_true",
        help="ط¬ط§غŒع¯ط²غŒظ†غŒ ظپط§ط²ظ‡ط§ ط¨ط§ ظ…ظ‚ط§ط¯غŒط± ظ¾غŒط´â€Œظپط±ط¶ (ظ…ظپغŒط¯ ط¨ط±ط§غŒ ط¨ط§ط²طھظˆظ„غŒط¯ ط§ط² طµظپط±).",
    )
    args = parser.parse_args(argv)

    result = run_sync(args)
    status = result["status"]
    applied = result["applied"]
    print(
        f"[roadmap_sync] overall={status.get('overall_progress')}% "
        f"milestones={status.get('completed_milestones')}/{status.get('total_milestones')} "
        f"applied={len(applied)} markers"
    )


if __name__ == "__main__":
    main()
