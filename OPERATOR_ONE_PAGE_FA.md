# راهنمای یک صفحه ای اپراتور (بدون کدنویسی)

1. بار اول: `install.bat`
2. اجرای روزانه: `start.bat`
3. توقف امن: `stop.bat`
4. صفحه اصلی: `http://localhost:8880`
5. داشبورد کنترل: `http://localhost:8880/developer`
6. در کارت اپراتور: `کپی آدرس داشبورد` (بعد از موفقیت: `آدرس کپی شد`) و `کپی نام فایل راهنما` (بعد از موفقیت: `نام فایل کپی شد`) برای کپی سریع
7. در راهنمای inline: `کپی کل چک لیست` (بعد از موفقیت: `چک لیست کپی شد`)
8. اگر پیام Clipboard دیدی: مجوز مرورگر را فعال کن و دوباره کپی بزن
9. اگر badge زرد بود (`Core: آنلاین (قدیمی)`): دکمه `بروزرسانی وضعیت`
10. اگر زرد ماند یا `Core: آفلاین` شد: `stop.bat` سپس `start.bat`
11. معیار سلامت: `Core آنلاین` + `Mission 6/6` + چت پاسخ می‌دهد
12. `امتیاز آمادگی`: سبز خوب، زرد نیازمند توجه، قرمز نیازمند اقدام فوری

یادداشت انتشار این نسخه: `OPERATOR_RELEASE_NOTES_FA_2026_03_08.md`

### مسیر بحران 4 مرحله ای (برای ارسال سریع به پشتیبانی)

1. Core را در `/developer` چک کن.
2. اگر Core زرد بود: `بروزرسانی وضعیت` را یک بار بزن.
3. اگر هنوز زرد/آفلاین بود: `stop.bat` سپس `start.bat`.
4. دوباره `/developer` را باز کن و `Mission 6/6` را تایید کن.

نکته: در راهنمای inline داخل داشبورد، دکمه `کپی فقط مراحل بحران (4 مرحله)` همین متن را کپی می‌کند (بعد از موفقیت: `مراحل بحران کپی شد`).
